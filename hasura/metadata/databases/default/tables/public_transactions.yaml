table:
  name: transactions
  schema: public
object_relationships:
  - name: space
    using:
      manual_configuration:
        column_mapping:
          space_id: space_id
        insertion_order: null
        remote_table:
          name: spaces
          schema: public
  - name: wallet
    using:
      manual_configuration:
        column_mapping:
          address: address
        insertion_order: null
        remote_table:
          name: wallets
          schema: public
