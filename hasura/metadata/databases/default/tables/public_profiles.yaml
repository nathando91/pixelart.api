table:
  name: profiles
  schema: public
object_relationships:
  - name: handlerByHandler
    using:
      foreign_key_constraint_on: handler
array_relationships:
  - name: accounts
    using:
      foreign_key_constraint_on:
        column: created_by
        table:
          name: accounts
          schema: public
